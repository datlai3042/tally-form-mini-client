import { InputCore } from "@/type";
import React from "react";

type TProps = {
	inputItem: InputCore.InputDate.InputTypeDate;
};

const InputCoreDate = (props: TProps) => {
	return <div>InputCoreDate</div>;
};

export default InputCoreDate;
