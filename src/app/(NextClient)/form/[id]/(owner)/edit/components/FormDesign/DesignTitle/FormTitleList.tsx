import { FormCore } from "@/type";
import React from "react";

type TProps = {
	subTitleItem: string;
};

const FormTitleList = (props: TProps) => {
	const { subTitleItem } = props;

	return <div>FormTitleList</div>;
};

export default FormTitleList;
