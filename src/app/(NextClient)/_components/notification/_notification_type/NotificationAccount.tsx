import { Notification } from "@/type";
import React from "react";

type TProps = {
	notification_item: Notification.Account.NotificationAccount;
};

const NotificationAccount = (props: TProps) => {
	return <div>NotificationAccount</div>;
};

export default NotificationAccount;
